# 06 — Pitches & Messaging

- [ ] Pitch composer: message + optional brief + budget
- [ ] Credit check: free cap → debit `credit_ledger` beyond limit; block if no credits
- [ ] Create `pitches` row (status sent); notify influencer
- [ ] Influencer inbox: list pitches (`pitches` by influencer_id, status)
- [ ] Accept / decline pitch → update status + `responded_at`
- [ ] On first message, create `threads`; realtime `messages` via Supabase Realtime
- [ ] Thread UI: bubbles, send box, unread state
- [ ] Credit purchase flow (₹1000 / 10 credits) — stub checkout in MVP
- [ ] Subscription tier gating (unlimited swipes / priority) — stub
